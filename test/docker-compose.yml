version: "3"
services:
  familie-tilbake-frontend:
    restart: always
#    depends_on:
#      - mock-oauth2-server
#      - familie-ba-sak
#      - familie-redis
    image: familie-tilbake-frontend
    ports:
      - 8000:8000
    environment:
      ENV: local
      APP_VERSION: ${APP_VERSION}
      SESSION_SECRET: ${SESSION_SECRET}
      CLIENT_ID: ${SAK_FRONTEND_CLIENT_ID}
      CLIENT_SECRET: ${SAK_FRONTEND_CLIENT_SECRET}
      FAMILIE_TILBAKE_SCOPE: ${FAMILIE_TILBAKE_SCOPE}
#      PASSPORTCOOKIE_KEY1: KEY1
#      PASSPORTCOOKIE_KEY2: KEY2
#      PASSPORTCOOKIE_KEY3: KEY3
#      PASSPORTCOOKIE_KEY4: KEY4
      COOKIE_KEY1: COOKIE_KEY1
      COOKIE_KEY2: COOKIE_KEY1
#      SLACK_TOKEN: SLACK_TOKEN